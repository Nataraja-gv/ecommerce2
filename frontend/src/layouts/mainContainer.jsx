import React from "react";

const MainContainer = ({ children }) => {
  return (
    <div className=' bg-gray-50  w-full pt-[80px] min-h-[100vh] '>{children}</div>
  );
};

export default MainContainer;
